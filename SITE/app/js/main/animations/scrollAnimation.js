'use strict';

/* scrollAnimation */


var scrollAnimation = function() {
	var animation = {};

	animation.event = function (elem, done) {
		// logic

		done();
	};

	return animation;	
};

module.exports = scrollAnimation;